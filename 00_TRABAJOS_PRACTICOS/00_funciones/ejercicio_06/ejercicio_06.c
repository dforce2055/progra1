/*
 * 6. La tarifa de un videoclub  para el alquiler de sus videos es: Dibujos = $2; Estrenos = $3; Otros = $2.5. Además cobra por devolución
 * tardía $1 por el primer día y $0.5 por cada uno de los días siguientes. Realizar una función que devuelva el alquiler a pagar por un
 * cliente, sabiendo que recibe como parámetros el tipo de video y la cantidad de días desfasados en devolución. Si la devolución es anterior
 * al día de devolución recibirá un entero negativo.
 * */
#include <stdio.h>

float alquiler_a_pagar(int tipo, int atraso);

int main(){
	
	
	return 0;
}
float alquiler_a_pagar(int tipo, int atraso)
{
	int dibujos = 2;
	int estrenos = 3;
	float otros = 2,5;
	float resultado = 0;
	
	resultado = dibujos +  
	
	
	return resultado;
}
